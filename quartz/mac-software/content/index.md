---
title: "Mac Softwares"
---

### As a Software Engineer, I usually install many development tools.

My current IDE is [[programs/dev-tool/VSCode]]. and use [[programs/language/nodejs]](`JavaScript` and `TypeScript`)  and [[programs/language/python]] as my primary programming languages. I am also interested in `Swift`, `Rust` and `Go` language.(but not good at)

I use [[programs/dev-tool/DockerDesktop]] for developing backend server. I usually uses `AWS` as my primary cloud service.
(On Linux, I additionally use `lxc` and `libvirt` for testing)

I use [[programs/dev-tool/DBeaver]] for SQL development.

I use both [[programs/base/Firefox]] and [[programs/base/Chrome]] as web browser.
Use `Chrome` as default browser, and use `Firefox` as my daily driver.
 
Mostly command line tools are installed via [[programs/base/homebrew]]. but these days many tools can be installed using `npm`

These days, I also install [[programs/ai/|AI]] tools to experiment AI softwares.

### As Hobby

I install [[programs/design/FreeCad]] to design a simple part for `3D Printing`

