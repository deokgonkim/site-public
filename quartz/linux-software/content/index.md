---
title: Linux Softwares
---
### As a Software Engineer, I usually install many development tools.

My current IDE is [[programs/dev-tool/VSCode]]. and use [[programs/language/nodejs]](`JavaScript` and `TypeScript`)  and [[programs/language/python]] as my primary programming languages. I am also interested in `Swift`, [[programs/language/rust]] and [[programs/language/go]] language.(but not good at)

I use [[programs/dev-tool/docker]] for developing backend server. I usually uses `AWS` as my primary cloud service.
I additionally use [[programs/dev-tool/lxd]] and [[programs/dev-tool/virt-manager]] for testing

I use [[programs/dev-tool/DBeaver]] for SQL development.

I use both [[programs/base/Firefox]] and [[programs/base/Chrome]] as web browser.
Use `Chrome` as default browser, and use `Firefox` as my daily driver.

On Linux, there are several methods to install package. `apt` for essential packages, `snap` for desktop applications, `flatpak` for desktop applications.  but these days many tools can be installed using `npm`

These days, I also install [[programs/ai|AI]] tools to experiment AI softwares.

I use [[programs/dev-tool/hugo]] [Quartz](https://quartz.jzhao.xyz/) for static web site generation.
### As Hobby

I install [[programs/design/Freecad]] to design a simple part for `3D Printing`  
I install [[programs/design/KiCad]] to design a simple `PCB`. someday I want to place order in `JLCPCB` or `PCBWAY`  
I installed [[programs/design/Blender]] few years ago. but not using at all these days.  
I install [[programs/video/Kdenlive]] for video editing. (some video uploaded on youtube)  
I install [[programs/video/OBS_Studio]] to screen recording.  
