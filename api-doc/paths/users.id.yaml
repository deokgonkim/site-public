# READ
get:
  tags:
    - User
  summary: Get user by Id
  description: >
    Returns user
  parameters:
    - $ref: '../schemas/parameters.yaml#/userId'
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/User'
# UPDATE
put:
  tags:
    - User
  summary: Update user by Id
  description: >
    Updates user
  parameters:
    - $ref: '../schemas/parameters.yaml#/userId'
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/User'
# DELETE
delete:
  tags:
    - User
  summary: Delete user by Id
  description: >
    Deletes user
  parameters:
    - $ref: '../schemas/parameters.yaml#/userId'
  responses:
    200:
      description: OK
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/User'
